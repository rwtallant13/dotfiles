#!/bin/bash



mp4paths=$((find . -regex ".*\.\(mkv\|mp4\)"))
mp4names=("${mp4paths[@]##*/}")
name=("${mp4names[@]%.*}")
srt=$((find . -regex ".*\.\(mkv\|mp4\)"))
for i in "${!srt[@]}"
do
printf '%s -> %s\n' "${srt[$i]}" dir2/"${name[$i]}".srt
done
