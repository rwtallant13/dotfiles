#
# ~/.aliases
#


# system
alias dots-tracked='yadm list -a|less'
alias dots='yadm'
alias services='systemctl list-unit-files | grep enabled'
alias grep='grep --color=auto'
alias J='sudo journalctl'
alias locate='plocate'
alias myip='curl http://ipecho.net/plain; echo'
alias S='sudo systemctl'
alias s='systemctl --user'
alias sudo='sudo '
alias sufw='sudo ufw'
alias su='sudo -i'
alias timers='systemctl list-timers'
alias udb='sudo updatedb.plocate'
alias udev-reload='sudo udevadm control --reload-rules && sudo udevadm trigger'


# pacman
alias fzpacr='sudo pacman -Rncs $(fzpkgs)'
alias pacdiff='sudo -E pacdiff'
alias pacf='sudo pacman -F'
alias pacl='sudo pacman -Ql'
alias pacq='sudo pacman -Qs'
alias pacrepo='sudo reflector --latest 10 --country US --protocol https --sort rate --save /etc/pacman.d/mirrorlist && sudo pacman -Syy'
alias pacr='pacman -Rncs'
alias pacs='sudo pacman -Ss'
alias pac='sudo pacman -S'
alias pacu='sudo pacman -Syu'
alias pacy='sudo pacman -Syy'
alias rmorphans='sudo pacman -Rns $(pacman -Qtdq)'
alias yay='paru'

#ls
alias l='ls -hN --color=auto --group-directories-first'
alias la='ls -hNa --color=auto --group-directories-first'
alias lla='ls -hlNa --color=auto --group-directories-first'
alias ll='ls -hlN --color=auto --group-directories-first'
alias ls='ls -hN --color=auto --group-directories-first'
alias lr='ls -RhN --color=auto --group-directories-first'
alias lb='ls -hN --color=auto --group-directories-first /home/rob/.bin/'

#commands
alias cmd='compgen -c | fzf'
alias cpuspeed='watch -n.1 "cat /proc/cpuinfo | grep \"^[c]pu MHz\""'
alias df='df -h'
alias gcp='g++'
alias hb='HandBrakeCLI'
alias hist='history'
alias iftop='sudo iftop -b'
alias m='micro'
alias ndu='nnf -C -T a'
alias n='nnf'
alias pb='curl -F "f:1=<-" ix.io'
alias rf='rg --files --hidden'
alias rm-empty='fd -t d -x rmdir --ignore-fail-on-non-empty {}'
alias rm='rm -I'
alias rmr='rm -rfI'
alias rmspaces='find . -name "* *" -type f | /usr/bin/vendor_perl/rename 's/ //g' '
alias tb='nc termbin.com 9999'
alias ticker='ticker --show-fundamentals --show-separator --config $HOME/.config/ticker.yaml'
alias vfb='vifm /media/blueberry/backups/rob-pc /home/rob'
alias vfm='vifm /media/blueberry/ /home/rob/downloads/torrents/ '
alias vf='vifm'
alias v='/home/rob/.bin/nv'
alias xc='xclip -selection clipboard'


# bb/pi
alias bb='ssh bb'
alias pilog='ssh -t pi pihole -t'
alias mcctrl='ssh -t bb /home/rob/bin/mcctrl'

mclog () {
	if [ $1 == '-a' ]
	then
		ssh bb journalctl -r -u minecraft@vanilla.service|less
	else
		ssh bb journalctl -f -u minecraft@vanilla.service
	fi
}

#
# functions
#

fzcd() {
	$(__fzf_cd__)
}

pacman-pkgs(){
	pacman -Qqne | tee /home/rob/docs/pacman-pkgs
}

aur-pkgs(){
	pacman -Qqme | tee /home/rob/docs/aur-pkgs
}

backup(){
	cp -b ${1} ${1}.bak
}

pkglist(){
	pacman -Qqme > /home/rob/docs/aur-pkgs
	pacman -Qqne > /home/rob/docs/pacman-pkgs
	pacman -Qqe | less
}

fzpkgs(){
    pacman -Qqe | fzf --height=100% --preview 'pacman -Qi {}' --layout=reverse
}

mkcd(){
	mkdir -p "$1" && cd "$1"
}

mkbin(){
	p=/home/rob/.bin/${1##*/}
	if [[ -n "$1" ]]; then
		printf '#!/bin/bash\n\n' > "$p" && chmod +x "$p" && nv "$p"
	else
		nv
	fi
}

chx(){
    chmod +x $*
}

vw(){
	viewnior $*
}

se(){
    dir="${@:-"$HOME"}"
    sel="$(fd . -H "$dir"| fzf)"
    if [[ -d "$sel" ]] ; then
        cd "$sel"
    elif [[ -f "$sel" ]]; then
        /home/rob/.bin/nv "$sel"
    fi
}

swap(){
    local TMPFILE=tmp.$$
    mv "$1" $TMPFILE && mv "$2" "$1" && mv $TMPFILE "$2"
}

open(){
    nohup xdg-open "$@" >/dev/null 2>&1 & disown
}

watchproc(){
    local pid

    pid=$(ps -e -o pid,user,cmd | sed 1d | fzf -m | awk '{print $1}')

    if [ "x$pid" != "x" ]
    then
        ps -p "$pid" -o etime,command
    fi
}

yf(){
    if [[ $1 ]]; then
        cat "$1" | xclip -selection clipboard
    else
        echo 'file required'
        exit 0
    fi

}

hist_dedup() {

    tac ~/.config/bash_history | awk '! x[$0]++' | tac > ~/bash_hist_clean

    mv ~/.config/bash_history ~/.config/bash_history_old

    mv ~/bash_hist_clean ~/.config/bash_history

}

fkill() {
    local pid

    pid=$(ps -eo user,pid,etime,cmd | sed 1d | fzf -m | awk '{print $2}')

    if [ "x$pid" != "x" ]
    then
        echo $pid | xargs sudo kill -${1:-9}
    fi
}

fzaux() {
    local pid
    if [ "$UID" != "0" ]; then
        ps -f -u $UID | sed 1d | fzf -m
    else
        ps -ef | sed 1d | fzf -m
    fi
}

fdchars() {
    fd -H $* | grep -E '\s|\*|\?|\$|%|'\ ''

}
